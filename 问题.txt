现在的问题 python代码独立的，没有根据上一个结果作为下一个的输入，就比如mcp得到了对应位置信息（景点或者餐厅）的结果，后面的python代码要去读取它，而不是现在编造数据， 最后还有一个问题就是现在的html输出位置，总是会在blocks上一级目录生成，但是检验的位置又是blocks。对于python代码最后的结果应该不是打印前十个，而是把数据按可读取的可是存放起来。并记住这些文件名字，方便后续的代码处理的数据是实时获取的而不是编造的。
还要求可以调用unplash api在html中插入图片。
改成多个agent版本。现在一个太混乱了。


第一个景点查询agent，查询完成之后，得到结果直接进行下一步了，没有让模型分析结果，思考添加缺少的景点，删除重复的节点，以及和景点无关的信息。补充必要的结果。

行程规划，将分析得到的结果填入python中得到对应格式


景点复查agent，如果attractions.json已经符合条件，就不用生成代码了，也不需要调用代码等硬约束条件，是不是可以让agent输出一个标识，来判断是否通过，不通过它输出的就是python，通过就直接怎么样？又有新的问题，只是删掉一些景点的话，代码里面也不需要必须包含mcp工具调用的内容，你要修复这样的问题，完整的考虑结果，只用读取和修改travel_codegen_agent 和 prompt_templates.py







